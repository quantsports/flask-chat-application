# def complex_math_chai(llm):
#     math_template = PromptTemplate(
#         input_variables=['prompt'],
#         template='Do the simple or complex math problem {prompt}'
#     )
#     math_chain = LLMMathChain(llm_chain=LLMChain(llm=llm), prompt=math_template, verbose=True, type=value_error.missing)
#     return math_chain
#
#
# chat_template = PromptTemplate(
#     input_variables=['prompt'],
#     template='Following is a friendly conversation between you and user. You are talkative and trying to give as detailed and accurate answers as you can {prompt}'
# )
